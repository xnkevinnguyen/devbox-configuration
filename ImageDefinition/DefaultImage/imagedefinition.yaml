$schema: 1.0
name: project-sample-debug
image: ~Gallery~Default~microsoftvisualstudio_windowsplustools_base-win11-gen2
tasks:
    - name: choco
      parameters:
        package: notepadplusplus.install
    - name: choco
      parameters:
        package: vscode.install
    - name: choco
      parameters:
        package: vscode.install # Duplicate task
    - name: choco
      parameters:
        package: firefox # Duplicate task


        
